import React from 'react';
import { Auth, ContainerView, Title } from './styles';

export const Main = () => {
  return (
    <ContainerView>
      <Title>That's my React-Native Boilerplate</Title>
      <Auth>by AndeCardoso</Auth>
    </ContainerView>
  );
};
