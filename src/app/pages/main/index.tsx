import React from 'react';
import { Layout } from '@components/layout';
import { HomeLayout } from './home';

export const Main = () => {
  return (
    <Layout>
      <HomeLayout />
    </Layout>
  );
};
