import React from 'react';
import { Title } from './styles';
import { Layout } from '@components/layout';

export const Main = () => {
  return (
    <Layout>
      <Title>Já saiu?</Title>
    </Layout>
  );
};
